import styled from "styled-components";

export const BodyOfPagesStyled = styled.div`
  margin: -3px 70px;
  background-color: var(--white);
  border: 1px solid var(--border);
  box-shadow: 0px 0px 4px rgba(0, 0, 0, 0.5);
`